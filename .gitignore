node_modules
.env
.env.local
.env.*.local

# Built agent specs are user artifacts; the committed examples/ folder shows the shape.
/agents/*.json
# (examples/ is committed on purpose — it's the demo)

*.tsbuildinfo

# minion working copies (regenerated each run)
/.minion-runs/
# minion receipts are artifacts; committed examples live in examples/
/minion-receipts/
/minion-evals/
# per-repo profiles are a local cache the minion rebuilds
/.minion-profiles/
# the outcome corpus is regenerated from receipts
/minion-corpus.json
